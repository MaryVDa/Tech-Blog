<div class="comments">
    {{#each comments}}
    <div>
        <p class="card-body">
            {{comment_content}} - {{user.username}} on {{comment_date}}
        </p>
    </div>
    {{/each}}
</div>